# Mentu Workspace Manifest
# Generated by: mentu workspace-init
name: "{{PROJECT_NAME}}"
project_title: "{{PROJECT_TITLE}}"
workspace_id: "{{WORKSPACE_ID}}"
actor: "{{ACTOR}}"
build_cmd: "{{BUILD_CMD}}"
completion_promise: "{{COMPLETION_PROMISE}}"
token_env_var: "{{TOKEN_ENV_VAR}}"
workspace_env_var: "{{WS_ENV_VAR}}"
domains: "{{PROJECT_DOMAINS}}"
dev_port: "{{DEV_PORT}}"
